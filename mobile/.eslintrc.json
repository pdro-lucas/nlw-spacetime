{
  "extends": ["@rocketseat/eslint-config/react"],
  "rules": {
    // Disable prettier because it's not working with prettier extension
    "prettier/prettier": "off",
    "camelcase": "off"
    }
}
